version: "3.9"

services:
  app:
    build: .
    environment:
      - UNIT_WORKERS=${UNIT_WORKERS:-4}
    expose:
      - "8080"

  lb:
    image: nginx:stable
    depends_on:
      - app
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
